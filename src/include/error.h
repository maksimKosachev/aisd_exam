#ifndef ERROR_H
#define ERROR_H

typedef enum Error {
	END_OF_FILE = -1,
	ALL_GOOD,
	MEMORY_ERROR,
	EMPTY_STRUCTURE,
	FULL_STRUCTURE,
	INCORRECT_KEY,
	KEY_NOT_FOUND,
	KEY_ALREADY_EXISTS,
} Error;

#endif
